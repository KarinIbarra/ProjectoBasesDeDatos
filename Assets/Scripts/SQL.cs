using UnityEngine;
using Mono.Data.Sqlite;

public class TestSQLite : MonoBehaviour
{
    void Start()
    {
        Debug.Log("SQLite OK"); // esto solo es para comprobar si habia conectado unity con SQL correctamente
    }
}